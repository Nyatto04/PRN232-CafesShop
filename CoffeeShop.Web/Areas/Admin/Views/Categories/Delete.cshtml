@model Shared.Dtos.CategoryDto
@{
    ViewData["Title"] = "Xác nhận Xóa Danh mục";
}

<h1>@ViewData["Title"]</h1>
<h3 class="text-danger">Bạn có chắc chắn muốn xóa (ẩn) danh mục này?</h3>

<div class="alert alert-warning">
    <strong>Lưu ý:</strong> Bạn chỉ có thể xóa nếu không có sản phẩm nào (đang hoạt động) thuộc danh mục này.
</div>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Tên danh mục</dt>
        <dd class="col-sm-10">@Model.CategoryName</dd>
        <dt class="col-sm-2">Mô tả</dt>
        <dd class="col-sm-10">@Model.Description</dd>
    </dl>

    <form asp-action="Delete" method="post">
        <input type="submit" value="Xóa" class="btn btn-danger" /> |
        <a asp-action="Index" class="btn btn-secondary">Hủy bỏ</a>
    </form>
</div>